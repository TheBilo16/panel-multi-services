import React from 'react';
import TStyles from './styles';

const Title = () => {
  return <TStyles.Container>
    <TStyles.Text>Multi Servicios</TStyles.Text>
  </TStyles.Container>
}

export default Title;