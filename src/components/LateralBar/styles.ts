import styled from "styled-components";

const LateralBarStyles = {
  MainContainer : styled.div`
    width: 350px;
    height: 100vh;
    background-color: #fff;
    display: grid;
    grid-template-rows: 100px 1fr 120px;
  `
}

export default LateralBarStyles;
